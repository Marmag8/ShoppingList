<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:ShoppingList.Views"
       xmlns:recipes="clr-namespace:ShoppingList.Views.Recipes"
       x:Class="ShoppingList.AppShell">
  <TabBar Shell.TabBarBackgroundColor="{DynamicResource Gray900}">
    <ShellContent
        Title="Lista Zakupów"
        Route="shopping"
        ContentTemplate="{DataTemplate views:ShoppingListPage}" />
    <ShellContent
        Title="Przepisy"
        Route="recipes"
        ContentTemplate="{DataTemplate recipes:RecipesPage}" />
  </TabBar>
</Shell>
